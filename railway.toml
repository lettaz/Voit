[build]
builder = "NIXPACKS"
buildCommand = "npx convex codegen && cd backend && pnpm install && pnpm run build"

[deploy]
startCommand = "cd backend && node dist/backend/src/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
